四、TODO ElasticSerach 使用流程：（倒排索引）
    索引 就像是库
    类型 就像是表  7版本以上就去掉了，为了提高检索效率 因为映射的引入
    文档 就像每一条数据

    分词 分词器 (可以通过配置指定一个我们自定的词库)
    相关性得分

    各种短语匹配
    多字段匹配等等

    bool复合查询
    结果过滤
    term 查询 精确值适合
    aggregations 执行聚合  提供了从数据中分组和提取数据的能力。

    #映射 mapping
    1、创建索引的时候 就给每个字段指定映射mapping规则
    2、已经存在的映射是不可以更新的，可以数据迁移

    使用 Java Rest Client
    Java hight level rest client

    要使用就必须先配置 client


    注意点：neted 类型的数据使用



五、TODO　缓存和分布式锁
１、本地缓存和分布式缓存
 2、redisson 锁框架 完整实现redis锁
    *可重入锁：指的就是A调用B 而这两个方法都需要加锁
    *（1、默认是30秒，锁的自动续期，如果业务超长，运行期间自动续期
        2、加锁业务只要运行完成，就不会给当前锁续期）| 看门狗机制
3、读写锁：
    *
    *
    *

六、TODO 远程调用失败后，数据不回滚的方式
1、try_cacth 的使用场景
2、还可以用什么办法让异常出现不回滚？ 高级部分学习
六、TODO　springboot2.0给我们带来了 webflux编程